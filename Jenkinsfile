pipeline{    agent {        docker { image 'maven' }    }    stages{        stage('build'){            //mvn构建            steps{                sh "ls -la"                sh "pwd"                sh "mvn clean install -Dmaven.test.skip=true"            }        }        stage('deploy'){            steps{                //执行部署脚本                echo "deploy ......"            }        }    }}